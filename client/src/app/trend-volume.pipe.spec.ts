import { TrendVolumePipe } from './trend-volume.pipe';

describe('TrendVolumePipe', () => {
  it('create an instance', () => {
    const pipe = new TrendVolumePipe();
    expect(pipe).toBeTruthy();
  });
});
